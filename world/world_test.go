package world

/*
func TestWorldIntersection(t *testing.T) {
	w := &World{}
	testCases := []struct {
		src sdl.Rect
		dst sdl.Rect
	}{
		{
			src: sdl.Rect{0, 0, 640, 480},
			dst: sdl.Rect{0, 0, 13, 10},
		},
		{
			src: sdl.Rect{100, 100, 640, 480},
			dst: sdl.Rect{2, 2, 13, 10},
		},
		{
			src: sdl.Rect{125, 125, 640, 480},
			dst: sdl.Rect{3, 3, 13, 10},
		},
	}

	for _, tc := range testCases {
		rect := w.Intersect(tc.src)

		if rect != tc.dst {
			t.Errorf("expected min %v, got %v\n", tc.dst, rect)
		}
	}

}
*/
